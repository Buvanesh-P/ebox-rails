<%= form_with(model: [post,post.comment.build]) do |f| %>

<div class="form-control mt-2">
     <%= f.rich_text_area :content%>
     <%= f.submit "Comment", class:"btn btn-primary mt-1"%>   
</div>
<% end %>