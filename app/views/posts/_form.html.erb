<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title, class: "required form-control" %>
        <%= form.rich_text_area :content,class:"required-rich-text" %>

  </div>
    <%= form.hidden_field :users_id, value: current_user.id%>

  <div>
    <div class="post-div mt-2 submit-warp"><%= form.submit %></div>
  </div>
<% end %>

