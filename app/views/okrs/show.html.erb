<div class="container mt-3">
  
  <%= link_to "Back", okrs_path,class:"btn btn-primary" %>
  <%if @okr.users_id == current_user.id%>
  <%= link_to "Edit", edit_okr_path(@okr),class:"btn btn-secondary" %> 
  <%end%>
  <%= render @okrview %>

  <%if @okr.users_id == current_user.id%>
  <%= button_to "Delete", @okr, method: :delete,class:"btn btn-danger mt-3" %>
  <%end%>
</div>
